{
  "name": "anne-test",
  "version": "1.0.8",
  "description": "npm包-自己制作（测试）",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cleanLib": "./node_modules/.bin/rimraf lib",
    "bableBuild": "./node_modules/.bin/babel src --out-dir lib",
    "prepublish": "npm run cleanLib && npm run bableBuild"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/daipianpian/anne-test.git"
  },
  "keywords": [
    "npm-anne-test"
  ],
  "author": "Anne (dai_pianpian@126.com)",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "rimraf": "^2.6.3"
  }
}
